apiVersion: v1
kind: Service
metadata:
  name: myservice
  namespace: demodeployment
  labels: 
    app: nginxservice
spec:
  type: NodePort
  selector:
    app: nginx

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
           

